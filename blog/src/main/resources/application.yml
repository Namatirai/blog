thymeleaf:
  enabled: true
  prefix: classpath:/templates/
  suffix: .html
  cache: false
  mode: HTML

  datasource:
    url: jdbc:postgresql://localhost:5330/blog
    username: namatirai
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  main:
    web-application-type: servlet